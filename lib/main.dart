import 'package:flutter/material.dart';
import 'day16/bottom_nav_app.dart';

void main() {
  runApp(const BottomNavApp());
  //runApp(MyWalletProfileUI());
  //runApp(RoutingApp());
  //runApp(DefaultLayoutApp());
  //runApp(ButtonApp());
  //runApp(ColorWork());
  //runApp(ListWork());
  //runApp(ContainerApp());
  //runApp(MyApp());
}
